server {
    listen 666 ssl;
    server_name ec2-13-41-205-113.eu-west-2.compute.amazonaws.com;

    ssl_certificate /etc/letsencrypt/cert.pem;
    ssl_certificate_key /etc/letsencrypt/key.pem;

    location / {
        proxy_pass http://field-to-fork-mvc:3000/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

server {
    listen 667 ssl;
    server_name ec2-13-41-205-113.eu-west-2.compute.amazonaws.com;

    ssl_certificate /etc/letsencrypt/cert.pem;
    ssl_certificate_key /etc/letsencrypt/key.pem;

    location / {
        proxy_pass http://field-to-fork-visuals:5000/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
