

## How to run on local host


1. Install the python libraries if you haven't:
``` bash
pip install pandas
pip install dash 
```

2. Navigate to the directory 'field-to-fork-visual' and run `python visuals.py`

3. The visuals will appear on the localhost, port specified in visuals.py (currently port=3500)